<% content_for :title do %>
  Brands
<% end %>
<div class="columns">
  <div class="column is-half">
    <table class="table">
      <thead>
        <tr>
          <th>Name</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @brands.each do |brand| %>
          <tr>
            <td><%= brand.name %></td>
            <td><%= link_to 'Edit', edit_brand_path(brand) %></td>
            <% if brand.vehicles.empty? %>
              <td><%= button_to 'Destroy', brand, method: :delete, data: { turbo_confirm: 'Are you sure?', turbo_method: :delete } %></td>
            <% else %>
              <td>Used in <%= brand.vehicles.count%> vehicles</td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
